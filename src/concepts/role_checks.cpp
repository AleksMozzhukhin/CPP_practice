#include "concepts/role_checks.hpp"

// Никакого исполняемого кода не требуется.
// Если какая-то роль нарушит контракт (не наследуется от IPlayer и т.п.),
// сборка упадёт на этапе компиляции из-за static_assert.
